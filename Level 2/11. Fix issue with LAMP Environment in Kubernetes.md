One of the DevOps team members was trying to install a WordPress website on a LAMP stack, which is deployed on a Kubernetes cluster. It was working well, and we could see the installation page a few hours ago. However, something seems to have gone wrong with the stack after the website went down. Please look into the issue and fix it:  

FYI, the deployment name is `lamp-wp` and it is using a service named `lamp-service`. Apache is using the default HTTP port, and the NodePort is `30008`. From the application logs, it has been identified that the application is facing some issues connecting to the database, in addition to other problems. Additionally, there are some environment variables associated with the pods, such as `MYSQL_ROOT_PASSWORD`, `MYSQL_DATABASE`, `MYSQL_USER`, `MYSQL_PASSWORD`, and `MYSQL_HOST`

Also, do not attempt to delete or modify any other existing components, such as deployment names, service names, types, labels, secrets and so on.  

`Note:` The `kubectl` utility on the `jump-host` has been configured to work with the Kubernetes cluster.

### Answer

1. Click on the App button in the top right corner. 
2. A web page with a `502 Bad Request` opens.
3. Lets investigate the deployment by describing it.

```
kubectl describe deployments.apps lamp-wp
```

4. Because we cannot currently access the Apache web page which, as per instructions, is hosted on the default HTTP port (80), will take a closer look at all container ports.

```
 Containers:
   httpd-php-container:
    Image:      webdevops/php-apache:alpine-3-php7
    Port:       80/TCP (httpd) 
    Host Port:  0/TCP (httpd)
    Environment:
      MYSQL_ROOT_PASSWORD:  <set to the key 'password' in secret 'mysql-root-pass'>  Optional: false
      MYSQL_DATABASE:       <set to the key 'database' in secret 'mysql-db-url'>     Optional: false
      MYSQL_USER:           <set to the key 'username' in secret 'mysql-user-pass'>  Optional: false
      MYSQL_PASSWORD:       <set to the key 'password' in secret 'mysql-user-pass'>  Optional: false
      MYSQL_HOST:           <set to the key 'host' in secret 'mysql-host'>           Optional: false
    Mounts:
      /opt/docker/etc/php/php.ini from php-config-volume (rw,path="php.ini")
   mysql-container:
    Image:      mysql:5.6
    Port:       3306/TCP (mysql)
    Host Port:  0/TCP (mysql)
    Environment:
      MYSQL_ROOT_PASSWORD:  <set to the key 'password' in secret 'mysql-root-pass'>  Optional: false
      MYSQL_DATABASE:       <set to the key 'database' in secret 'mysql-db-url'>     Optional: false
      MYSQL_USER:           <set to the key 'username' in secret 'mysql-user-pass'>  Optional: false
      MYSQL_PASSWORD:       <set to the key 'password' in secret 'mysql-user-pass'>  Optional: false
      MYSQL_HOST:           <set to the key 'host' in secret 'mysql-host'>           Optional: false
    Mounts:                 <none>
```

5. The deployment has two containers and the first one is open on port 80. 
6. The instructions indicate that there is a lamp-service being used so will investigate that next.

```
kubectl get service

# output
NAME            TYPE        CLUSTER-IP      EXTERNAL-IP   PORT(S)          AGE
kubernetes      ClusterIP   10.43.0.1       <none>        443/TCP          53m
lamp-service    NodePort    10.43.116.133   <none>        8080:30008/TCP   8m36s
mysql-service   ClusterIP   10.43.50.4      <none>        3306/TCP         8m36s
```

7. We have identified the lamp-service and under ports we see that its set to port 8080 and not 80.
8. Will create a manifest of the service by exporting the output to a yaml file.

```
kubectl get service lamp-service -o yaml > lamp-service.yaml
```

9. Open the file to edit and we can see that the port and target port are set to `8080`.
10. As the container is listening on port `80` we need to change both ports to `80`.
11. After making the changes, apply the updated configuration.

```
kubect apply -f lamp-service.yaml
```

12. Now we click on the App button in the top right and we get a new error message `Unable to Connect to ''`.
13. This indicates that we are now able to connect to the container but the container is unable to connect to the MySQL database to server the content. 
14. Lets look at the looks to investigate why the app is not able to connect to the database.

```
kubectl logs deployments/lamp-wp

# Output
[26-Jun-2026 06:09:18] WARNING: [pool www] child 222 said into stderr: "NOTICE: PHP message: PHP Notice:  Undefined index: MYSQL_PASSWORDS in /app/index.php on line 4"
[26-Jun-2026 06:09:18] WARNING: [pool www] child 222 said into stderr: "NOTICE: PHP message: PHP Notice:  Undefined index: HOST_MYQSL in /app/index.php on line 5"
[26-Jun-2026 06:09:18] WARNING: [pool www] child 222 said into stderr: "NOTICE: PHP message: PHP Warning:  mysqli_connect(): (HY000/2005): Unknown MySQL server host 'mysql' (-2) in /app/index.php on line 8"
[php-fpm:access] 127.0.0.1 -  26/Jun/2026:06:09:18 +0000 "GET /index.php" 200 /app/index.php 4.156 2048 0.00%
[Fri Jun 26 06:09:18.703269 2026] [proxy_fcgi:error] [pid 122:tid 138791191878320] [client 10.22.0.1:1957] AH01071: Got error 'PHP message: PHP Notice:  Undefined index: MYSQL_PASSWORDS in /app/index.php on line 4\nPHP message: PHP Notice:  Undefined index: HOST_MYQSL in /app/index.php on line 5\nPHP message: PHP Warning:  mysqli_connect(): (HY000/2005): Unknown MySQL server host 'mysql' (-2) in /app/index.php on line 8\n', referer: https://gh4iqh3a2jiuk37g.labs.kodekloud.com/
```

15. From the logs we can identify, that environmental variables are not defined in the `app/index.php` file therefore, the app is unable to connect to the database. 
16. Lets connect to the `httpd-php-container` and investigate the `index.php` file.

```
kubectl exec -it deployments/lamp-wp -c httpd-php-container -- bash
```

**Note** - we are executing and interactive shell `-it` in the container `-c httpd-php-container` where the `index.php` file located.

```
# open the file in the vi editor
vi app/index.php

# file contents
<?php
$dbname = $_ENV['MYSQL_DATABASE'];
$dbuser = $_ENV['MYSQL_USER'];
$dbpass = $_ENV['MYSQL_PASSWORDS']; # should be password not passwords
$dbhost = $_ENV['HOST_MYQSL']; # should be MYSQL_HOST not HOST_MYSQL


$connect = mysqli_connect($dbhost, $dbuser, $dbpass) or die("Unable to Connect to '$dbhost'");

$test_query = "SHOW TABLES FROM $dbname";
$result = mysqli_query($test_query);

if ($result->connect_error) {
   die("Connection failed: " . $conn->connect_error);
}
  echo "Connected successfully";bash-4.3# 
```

17. Make the changes to the environment variables and save and close the file.
18. Restart the `php-fpm` service for the changes to take affect and check the status of the service after the restart to ensure it is running again.

```
service php-fpm restart

# output
php-fpm:php-fpmd: stopped
php-fpm:php-fpmd: started

# Ensure service is running
service php-fpm status

# output
php-fpm:php-fpmd               RUNNING     pid 244, uptime 0:01:05 
```

19. Exit the container and press the App button on the top right to view the web page.
20. If the changes have implement successfully, the web page will show `Connected successfully`.
21. Click Check to complete the task.

**Note**

If the web page shows the following message below; do not click check. This message will cause the task to fail. Only press check if `Connected successfully` is displayed. 

```
# Index of /

Apache/2.4.25 (Unix) Server at 30008-port-gh4iqh3a2jiuk37g.labs.kodekloud.com Port 80
```
